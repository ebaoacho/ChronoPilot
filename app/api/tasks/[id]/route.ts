import { NextResponse } from "next/server";import { createSupabaseServer,requireUser } from "@/lib/supabase/server";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser();const {id}=await params;const body=await request.json() as {status?:string};if(user.demo)return NextResponse.json({id,...body});const db=await createSupabaseServer();const {data,error}=await db!.from("tasks").update({status:body.status,completed_at:body.status==="completed"?new Date().toISOString():null}).eq("id",id).eq("user_id",user.id).select().single();if(error)throw error;return NextResponse.json(data)}catch{return NextResponse.json({error:"更新できませんでした"},{status:400})}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser();const {id}=await params;if(!user.demo){const db=await createSupabaseServer();const {error}=await db!.from("tasks").delete().eq("id",id).eq("user_id",user.id);if(error)throw error}return new NextResponse(null,{status:204})}catch{return NextResponse.json({error:"削除できませんでした"},{status:400})}}

import {NextResponse} from "next/server";import {z} from "zod";import {createSupabaseServer,requireUser} from "@/lib/supabase/server";
const schema=z.object({title:z.string().trim().min(1).max(200),goal:z.string().max(2000).default(""),deadline:z.string().datetime().nullable().optional()});
export async function GET(){try{const user=await requireUser();if(user.demo)return NextResponse.json([]);const db=await createSupabaseServer();const {data,error}=await db!.from("projects").select("*").order("created_at",{ascending:false});if(error)throw error;return NextResponse.json(data)}catch{return NextResponse.json({error:"取得できませんでした"},{status:401})}}
export async function POST(request:Request){try{const user=await requireUser();const input=schema.parse(await request.json());if(user.demo)return NextResponse.json({id:crypto.randomUUID(),...input});const db=await createSupabaseServer();const {data,error}=await db!.from("projects").insert({user_id:user.id,...input}).select().single();if(error)throw error;return NextResponse.json(data,{status:201})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"登録できませんでした"},{status:400})}}

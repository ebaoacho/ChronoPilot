"use client";import {useState} from "react";
export function Onboarding({show}:{show:boolean}){const [open,setOpen]=useState(show);const [busy,setBusy]=useState(false);if(!open)return null;async function submit(form:FormData){setBusy(true);const body={targetSleepMinutes:Number(form.get("sleep")),morningPrepMinutes:Number(form.get("morning")),defaultTravelMinutes:Number(form.get("travel")),weekdayGameMinutes:Number(form.get("weekdayGame")),holidayGameMinutes:Number(form.get("holidayGame")),engineerVision:String(form.get("vision"))};const response=await fetch("/api/onboarding",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});setBusy(false);if(response.ok)setOpen(false)}return <div role="dialog" aria-modal="true" style={{position:"fixed",zIndex:100,inset:0,background:"rgb(0 0 0 / .65)",overflow:"auto",padding:16}}><form action={submit} className="card" style={{maxWidth:580,margin:"30px auto"}}><div className="eyebrow">3分で開始</div><h1>ChronoPilotをあなた向けに</h1><p className="muted">詳細はあとから変更できます。安全な初期値を入れてあります。</p><div className="metric-grid"><div className="field"><label>通常の睡眠（分）</label><input name="sleep" type="number" defaultValue="420"/></div><div className="field"><label>朝の準備（分）</label><input name="morning" type="number" defaultValue="52"/></div><div className="field"><label>よく行く場所への移動（分）</label><input name="travel" type="number" defaultValue="30"/></div><div className="field"><label>平日のゲーム（分）</label><input name="weekdayGame" type="number" defaultValue="90"/></div><div className="field"><label>休日のゲーム（分）</label><input name="holidayGame" type="number" defaultValue="150"/></div></div><div className="field"><label>目指すエンジニア像</label><textarea name="vision" defaultValue="大規模なAIサービスを設計し、継続的に価値を届けられるエンジニア"/></div><button className="button" disabled={busy}>{busy?"保存中…":"この設定で始める"}</button></form></div>}

import {describe,expect,it} from "vitest";import {dailyPlanResultSchema,naturalAddResultSchema} from "@/lib/domain/schemas";
describe("AI response validation",()=>{it("rejects inverted time blocks",()=>{expect(()=>dailyPlanResultSchema.parse({summary:"x",blocks:[{title:"bad",kind:"task",startsAt:"2026-01-01T11:00:00.000Z",endsAt:"2026-01-01T10:00:00.000Z"}],warnings:[]})).toThrow()});it("defaults safe decomposition fields",()=>{const result=naturalAddResultSchema.parse({preparationTasks:[]});expect(result.googleCalendarCandidate).toBe(false);expect(result.travelMinutes).toBe(0)})});
